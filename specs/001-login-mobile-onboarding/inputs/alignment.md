# Spec-kit 输入：登录页改造 + 移动端 + Onboarding（UX 版）

> **归档说明**：本文件是 spec-kit feature `001-login-mobile-onboarding` 的对齐输入文档。spec.md / plan.md / tasks.md 等已基于本文件生成。保留作为历史溯源，不再用于新一轮 /specify。
>
> **对齐时间**：2026-05-03
> **本版定位**：Demo / PoC / Onboarding 体验完善 + 移动端形态，**纯 UX**。公网部署相关（数据隔离、限流、Prompt Injection 防护、运维脚本等）**全部推后到下一发版**。
> **决策来源**：和杨堃完整对齐业务边界后定稿

---

## 一、增量目标

让目前已完成 14 Phase 的 SFA CRM **本地体验形态足够好**——PC 登录页 + 移动端 + 首次登录引导都做到能直接拿出去演示的水准。

为后续"挂公网给公众号读者试玩"做好 **UX 层面的准备**，但本版**不涉及**公网部署、安全加固、运维脚本。

**核心约束：**
- 目标观众是杨堃的读者（B 端 PM 为主，少量 CRM PM/研发负责人/业务老板），**不是 CRM 目标客户**
- 登录页不喊买课/咨询，构建心智为主（参考 `memory/project_main.md` 内容策略）
- 这是项目 `阶段 1：奠基` 的 UX 交付物（参考 `memory/project_main.md` 阶段 1）

---

## 二、范围

### ✅ 本次必做（纯 UX）

1. **PC 端登录页改造**
2. **移动端形态完整出**（不是只做登录，全功能移动版）
3. **首次登录引导**（PC + 移动两端）

### ❌ 本次不做（推后到"公网上线"发版）

**公网部署 / 安全 / 运维相关——全部推后：**
- 数据隔离机制（30 分钟重置 / per-session 独立 DB 等）
- API Key 限流（slowapi、消息数 / token 上限等）
- Prompt Injection 防护（system prompt 加固、工具白名单、审计日志等）
- IP rate limit / 防刷
- PV/UV 统计
- 云主机选型 / 部署脚本 / Nginx 配置 / SSL
- 重置脚本（`reset-demo.bat` 自动化触发）

**其他推后：**
- GitHub README 门面化
- Session 10 集大成文章
- Dashboard 模块在移动端的呈现（手机端不放，需要时塞"我的"）
- Admin 模块在移动端（演示不涉及）

---

## 三、完整业务决策

### 1. PC 端登录页

**信息架构（自上而下）：**

```
[Hero 区]
  主标题：这是一个用 AI 从零写到上线的 Native AI CRM
  （副标题暂不需要）

[试玩入口区 ← 视觉重心]
  ┌─────────────────────┐  ┌─────────────────────┐
  │ 销售·王小明           │  │ 销售经理·陈队长        │
  │ 一线销售视角：         │  │ 管理视角：             │
  │ 用 Chat 跟进客户、    │  │ 问一句"谁在偷懒"，     │
  │ 查询线索、自动预填表单 │  │ AI 帮你看清团队跟进节奏│
  │ [一键登录]           │  │ [一键登录]            │
  └─────────────────────┘  └─────────────────────┘

[项目亮点区]
  点题：这不是一次玩具实验，是一场对传统软件工程的正面挑战。

  🎬 VibeCoding × Spec Coding
  14 个 Phase、132 次 commit，AI 协助完成全部编码。
  不是"想到哪写到哪"——每个增量先把决策结构化
  （specify → plan → tasks → implement），再让 AI 编码。
  用文档约束生成行为，避免 AI 决策漂移。

  🧬 Palantir Ontology 落地
  在系统底层把业务对象、关系、可执行动作（Action）显式建模，
  让 AI 真正理解业务，而不是把业务知识塞进 system prompt。

  🤝 Copilot 人机协同
  所有界面 API 化，AI Agent 与人协同操控同一套系统——
  AI 不靠"模拟点击"，而是直接调接口，各司其职。

  ——
  GitHub 源码：github.com/pmYangKun/sfa-crm
  系列文章：公众号「pmYangKun」搜 "VibeCoding"
```

**关键约束：**
- **仅卡片登录**，不保留手动账号密码输入框
- 卡片点击 = 自动填账号密码并登录
- 卡片角色名严格用系统现有：sales01（王小明）、manager01（陈队长）
- 亮点区**没有二维码**，公众号「pmYangKun」用文字引导
- "数字"（14 Phase / 132 commit）保留，增加可信度
- 主标题文案保留对比张力（"对传统软件工程的正面挑战"），用于媒体传播抓眼球

### 2. 移动端形态

**实现方式：**
- **单代码库 + Next.js 响应式**
- **Layout 独立组件**：移动端独立的 layout（金刚区 + 全屏 chat），PC 保持现有（sidebar + 右侧 chat 栏）
- 业务页面尽量复用，仅 layout 和登录页是两套

**移动端登录页（简化版）：**
- Hero 标题保留
- 双角色卡片**垂直堆叠**（不是横向）
- 项目亮点区**精简版**（三段都展开，但每段只 1 行核心句）：
  ```
  🎬 VibeCoding × Spec Coding
     AI 全程编码 + 文档约束生成，避免决策漂移

  🧬 Palantir Ontology 落地
     业务对象 / 关系 / Action 在底层显式建模

  🤝 Copilot 人机协同
     界面全 API 化，AI 直接调接口而不是模拟点击

  公众号「pmYangKun」全程记录
  ```
- **GitHub 链接去掉**（手机不 clone）

**金刚区主导航（5 入口，中间凸起）：**

```
                    [Chat 主屏]
                    
       线索   客户   [💬]   跟进   我的
                    ↑凸起
```

| 入口 | 内容 |
|------|------|
| 线索 | Lead 列表（按 DataScope 自动过滤） |
| 客户 | Customer 列表 |
| 💬 AI 助手 | **凸起按钮**，全屏 Chat 模式 |
| 跟进 | 最近跟进流 + 录入入口 |
| 我的 | 账号信息 + **一键切换角色** + 退出 |

**关键约束：**
- **登录后默认进入 Chat 全屏**（点中间凸起按钮的状态）
- "我的" tab 必须支持**一键角色切换**（sales01 ↔ manager01），无需重新输密码

**移动端 Copilot 写类操作交互（chat 内嵌卡片范式）：**

PC 端的"chat 右侧 + form 左侧"双栏模式在移动端无法照搬（全屏 chat 没有可引导的左侧表单）。移动端写类操作（创建客户/联系人/跟进等）采用 **chat 内嵌卡片** 范式，对标 ChatGPT canvas / Claude artifact：

- AI 完成预填后，在 chat 流里**渲染表单预览卡片**，每个对象（客户、联系人、跟进）一张卡
- 卡片显示：对象类型 + 关键字段预览（如"客户：深圳前海微链 / 联系人：赵鹏 / 跟进：拜访合作方案"）+ 状态徽章
- **点卡片** → 弹**底部抽屉（bottom sheet）**展示完整可编辑表单
- 抽屉里点"确认提交" → 关闭抽屉 → 卡片状态变 ✅ 已创建，并显示创建成功的对象 ID/链接
- 多对象批量创建 → 多张卡片在 chat 里**纵向堆叠**，状态各自独立
- 用户可以继续在 chat 追问"再补一条备注" → AI 再追加一张卡片

**为什么选这个范式：**
- 不打断 chat 上下文（用户能持续追问）
- 多对象天然叠多张卡片，比"全屏路由跳 3 次"或"sheet 串行弹"体验好
- 移动端访客大概率没用过 SFA CRM 但用过 ChatGPT，看一眼就懂
- 这个交互本身就是"Native AI CRM"亮点的可视化证据

**新增组件（spec-kit /plan 阶段需识别）：**
- `<ChatFormCard>`：chat 流内的表单预览卡片，含 pending/editing/submitted 三态
- `<MobileFormSheet>`：底部抽屉表单，复用现有表单字段组件
- AI 工具调用层：写类 tool 返回结构化"待确认对象"而非直接写库，由前端卡片驱动用户确认后再 commit

### 3. 演示引导（PC + 移动）

**引导卡片机制：**

- 卡片点击 → 问题自动发送到 Chat 并执行（**一键发送**，零摩擦）
- 实现使用**现有的 sessionStorage 模式**（chat 导航预填已经在用，延用）
- **永远显示**（demo 性质，没有"老用户"概念，每次进入 chat 都看到）

**PC 端引导位置：方案 B**

- 登录后默认进 **Dashboard**（现有页面）
- Dashboard 顶部加"AI 引导区"：欢迎语 + 卡片网格 + 切换角色提示
- 下方保留正常 Dashboard 数据

**移动端引导位置：**

- 登录后默认进 Chat 全屏
- Chat 首屏顶部显示推荐问题卡片（无消息时显示，有消息后折叠或消失）

**引导卡片清单：**

| 角色 | # | PC | 移动 | 标题 | 对应 demo case |
|------|---|----|----|------|--------------|
| sales01 | 1 | ✅ | ✅ | 🔍 自然语言查线索 | 案例 1（入门） |
| sales01 | 2 | ✅ | ✅ | ⚡ 一句话搞定多个动作 | 案例 3（王炸） |
| sales01 | 3 | ✅ | ✅ | 🧠 让 AI 给我跟进策略 | 案例 4 |
| sales01 | 4 | ✅ | ✅ | 💡 想看队长视角？切换 manager01 | 一键切换 |
| manager01 | 1 | ✅ | ✅ | 🚨 谁在偷懒？AI 一眼看穿 | 案例 6（王炸） |
| manager01 | 2 | ✅ | ✅ | 🔬 评估具体线索跟进情况 | 案例 8 |
| manager01 | 3 | ✅ | ✅ | 💡 想看销售视角？切换 sales01 | 一键切换 |

> **移动端裁剪策略：** 上线后看实际使用数据（哪些卡片点击率低 / 哪些用户滑过去就走）再决定裁哪张，不预先砍。

**卡片视觉格式（参考）：**

```
┌─────────────────────────────────────────┐
│  ⚡ 一句话搞定多个动作                    │
│  "我今天拜访了深圳前海微链的赵鹏，         │
│   聊了合作方案，对方很感兴趣。            │
│   帮我把客户、联系人、跟进都录进去。"      │
│  [试试看 →]                             │
└─────────────────────────────────────────┘
```

短标题 + 完整问题预览 + 一键发送按钮。

> **文案原则：** 给访客（B 端 PM，但不是 CRM 用户）看的话术必须把"AI 干了什么"具体化——避免"该做的都做了"这种行话，要把对象点出来（"客户、联系人、跟进"）。`docs/copilot-cases.md` 案例 3 原话已同步更新。

**"切换角色"卡片：**
- 点击 → 弹确认框 → 退出当前账号 → 自动登录目标账号 → 进入对应引导
- 后端需要新增"快速切换"API（或者前端调登录 API + 当前 logout API）

**"谁在偷懒"案例呈现（方案 A 文字加强版）：**
- AI 回答用 markdown 表格 + 关键数字加粗 + emoji（🔴⚠️✅）
- 每个销售一行：姓名 + 跟进活跃度评级 + 风险线索数
- 不做富组件嵌入（B 方案推后到主题攻势"对话式 CRM"再做）

---

## 四、下次进 spec-kit 的执行建议

1. **新会话开场**：告诉 Claude「我们要为 SFA CRM 增量做登录页改造 + 移动端 + 首次登录引导（纯 UX 版，不涉及部署 / 安全），业务决策全部在 `specs/001-login-mobile-onboarding/inputs/alignment.md`，跑 spec-kit `/specify`」
2. **/specify 阶段**：基于本文档生成 spec.md。如遇 `[NEEDS CLARIFICATION]`，多数应该已经在本文档里有答案
3. **/plan 阶段**：技术方案，关注：
   - Layout 独立组件如何拆（mobile/desktop）
   - 一键切换角色的 API（前端只调现有 logout + login，还是后端加新 API）
   - 移动端 chat 内嵌卡片范式（`<ChatFormCard>` / `<MobileFormSheet>` / 写类 tool 返回"待确认对象"的改造）
4. **/tasks 阶段**：任务拆解
5. **/implement 阶段**：写代码（业务结构层）
6. **跑起来后**：视觉细节、卡片话术、emoji、文案微调——不走 spec-kit，直接让 AI 改 CSS / 改卡片配置数组

---

## 五、参考资源（spec-kit 可能要查的）

- 项目主记忆：`memory/project_main.md`
- 现有 spec：`specs/master/spec.md`
- 现有数据模型：`specs/master/data-model.md`
- 早期设计文档：`specs/master/inputs/`（已归档，含 business-context / ontology / specifications）
- 演示案例集：`docs/copilot-cases.md`（8 个 case 详情）
- 现有 chat 导航预填实现：`src/frontend/.../chat-sidebar.tsx`（handleNavigate + sessionStorage）
